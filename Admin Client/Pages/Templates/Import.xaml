<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CertificateManager.Admin.Pages.Templates.Import"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CertificateManager.Admin.Pages.Templates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Manage Certificate Template"
           Style="{StaticResource TitleTextBlockStyle}"
           Grid.Row="0" />

        <StackPanel Orientation="Horizontal" Spacing="12" Grid.Row="1">
            <Button x:Name="ImportButton"
            Content="Import Certificate Templates"
            Click="ImportButton_Click"
            Style="{StaticResource AccentButtonStyle}"
            Padding="24,8" />

            <ProgressRing x:Name="ImportProgressIndicator"
                          IsActive="False"
                          Width="24" Height="24"
                          VerticalAlignment="Center" />
        </StackPanel>

        <Grid Grid.Row="2" RowSpacing="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="Import Log" Style="{StaticResource BodyStrongTextBlockStyle}" Grid.Row="0" />
            <ScrollViewer x:Name="ImportLogScroller"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Grid.Row="1">
                <RichTextBlock x:Name="ImportLog"
                               TextWrapping="Wrap"
                               IsTextSelectionEnabled="True" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
