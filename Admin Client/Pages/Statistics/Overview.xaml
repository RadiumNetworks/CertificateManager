<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Certificate_Manager.Pages.Statistics.Overview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Certificate_Manager.Pages.Statistics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Certificate Statistics"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Grid.Row="0" />

        <StackPanel Orientation="Horizontal" Spacing="12" Grid.Row="1">
            <NumberBox x:Name="MaxExpirationMonths"
                       Header="Max expiration (months)"
                       Value="2"
                       Minimum="1"
                       Maximum="120"
                       SpinButtonPlacementMode="Inline"
                       Width="200" />

            <Button x:Name="QueryButton"
                    Content="Load Statistics"
                    Click="QueryButton_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="24,8"
                    VerticalAlignment="Bottom" />

            <ProgressRing x:Name="ProgressIndicator"
                          IsActive="False"
                          Width="24" Height="24"
                          VerticalAlignment="Bottom"
                          Margin="0,0,0,6" />
        </StackPanel>

        <ListView x:Name="ResultsListView"
                  Grid.Row="2"
                  SelectionMode="None">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,8" Background="{ThemeResource SystemControlBackgroundListLowBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="300" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Count" FontWeight="Bold" Grid.Column="0" />
                        <TextBlock Text="Template Name" FontWeight="Bold" Grid.Column="1" />
                        <TextBlock Text="Template OID" FontWeight="Bold" Grid.Column="2" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TemplateStatistic">
                    <Grid Padding="12,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="300" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind UseCount}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind CN}" Grid.Column="1" />
                        <TextBlock Text="{x:Bind MsPKICertTemplateOID}" Grid.Column="2" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
